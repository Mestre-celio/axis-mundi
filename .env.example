# ============================================================
# AXIS MUNDI — REFERÊNCIA CENTRAL
# Este arquivo é APENAS uma referência de todas as variáveis.
# Para usar, copie os valores para:
#   backend/.env    (chaves secretas)
#   frontend/.env.local  (chaves públicas NEXT_PUBLIC_*)
# ============================================================
# Veja backend/.env.example e frontend/.env.local.example
